import React from 'react';

const API = () => {

    return (
        <div>
            This will become a page where I do a bunch of silly stuff with APIs. No clear plans so far, but I know Spotfy's api is frickin awesome. Might make my own spotify GUI type thing if possible?
        </div>
    );
}

export default API;